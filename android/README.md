# TINTA Android APK

This directory contains the Android WebView wrapper for the TINTA application.

The release build is produced by GitHub Actions and the APK is uploaded as a workflow artifact.

Set `TINTA_WEB_URL` in the Gradle build configuration to the deployed TINTA web application URL before publishing a production APK.
